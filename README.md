# Man O' War Game Companion — SSC02C R2 test-preview site

Status: `NOT PUBLISHED`.

This directory is prepared for a dedicated test GitHub Pages repository. It is not a production release, contains no `CNAME`, does not activate `https://game-companion.mowfleetbuilder.com/`, and excludes the diagnostic build.

The GitHub Pages URL and repository must be recorded at publication. The site is publicly reachable by anyone who obtains the URL; `robots.txt` is a crawler request, not access control.
